<?php
/**
 * Sidebar
 *
 * This template can be overridden by copying it to yourtheme/music-press-pro/global/sidebar.php.
 *
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly
}

get_sidebar( 'musicpress' );

/* Omit closing PHP tag at the end of PHP files to avoid "headers already sent" issues. */
